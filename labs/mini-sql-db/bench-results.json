{
  "hardware": "M2 MacBook Pro, 16GB RAM",
  "benchmarks": [
    {
      "metric": "SQL parse (SELECT with WHERE)",
      "value": "2,100,000",
      "unit": "parses/sec",
      "note": "estimated — tokenizer + recursive descent parser; run cargo bench for real numbers"
    },
    {
      "metric": "SeqScan (10k rows, no filter)",
      "value": "8,500,000",
      "unit": "rows/sec",
      "note": "estimated — iterator over Vec<Row> in memory"
    },
    {
      "metric": "Index scan (equality on 10k rows)",
      "value": "12,000,000",
      "unit": "lookups/sec",
      "note": "estimated — BTreeMap::get O(log n) vs full table walk"
    },
    {
      "metric": "NestedLoopJoin (100 x 100 rows)",
      "value": "45,000",
      "unit": "joins/sec",
      "note": "estimated — O(n^2) cross product; joins are expensive without a hash join"
    }
  ]
}
