container-lab:
  image: postgres:16
  database: labdb
  startup-timeout-seconds: 60

spring:
  main:
    web-application-type: none   # CLI demo — no HTTP server needed

management:
  endpoints:
    web:
      exposure:
        include: health,info
