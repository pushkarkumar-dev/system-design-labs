vector:
  base-url: http://localhost:8088   # Rust vector index HTTP server
  default-k: 10
  default-ef: 50                    # HNSW ef_search — tune for recall vs speed
  timeout: 5s

spring:
  application:
    name: vector-spring-integration

management:
  endpoint:
    health:
      show-details: always
