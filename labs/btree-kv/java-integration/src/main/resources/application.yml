btree:
  base-url: http://localhost:8080   # Rust B+Tree KV server URL
  cache:
    max-entries: 10000              # Cache up to 10k hot keys in Caffeine
    ttl: 10m                        # Evict after 10 minutes of inactivity

spring:
  application:
    name: btree-demo
  cache:
    type: caffeine

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always          # Exposes engine name, cacheHitRate, cacheSize

logging:
  level:
    dev.pushkar: DEBUG
