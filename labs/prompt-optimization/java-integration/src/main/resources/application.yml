spring:
  ai:
    openai:
      api-key: not-needed-for-local-demo
      base-url: http://localhost:8000

prompt-opt:
  base-url: http://localhost:8000
  max-llm-calls: 500
  num-trials: 10
  max-bootstrapped-demos: 5

logging:
  level:
    dev.pushkar: INFO
    org.springframework.ai: WARN
