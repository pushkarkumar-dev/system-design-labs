crdt:
  base-url: http://localhost:8090
  node-id: java-node

spring:
  application:
    name: crdt-spring-integration

server:
  port: 8091

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always
