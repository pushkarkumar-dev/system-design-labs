doc-db:
  base-url: http://localhost:8080   # Rust document DB server

spring:
  application:
    name: docdb-spring-integration

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    dev.pushkar.docdb: INFO
