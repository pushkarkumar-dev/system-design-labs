spring:
  data:
    redis:
      host: localhost
      port: 6379

cron:
  redis-host: localhost
  redis-port: 6379

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    dev.pushkar.cron: DEBUG
    net.javacrumbs.shedlock: DEBUG
