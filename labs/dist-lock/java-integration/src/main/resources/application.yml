dist-lock:
  service-url: http://localhost:8080   # Go lock server
  default-ttl-ms: 5000
  retry-attempts: 3
  retry-delay-ms: 100

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    dev.pushkar.lock: DEBUG
